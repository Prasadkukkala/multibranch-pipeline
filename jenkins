pipeline {
    agent any

    stages {
        stage('Hotfix branch') {
            steps {
                sh 'echo "the master branch"'
            }
        }
        stage('sprint1') {
            steps {
                sh 'echo "the sprint1"'
            }  
        } 
        stage('deployment') {
             steps {
                 sh 'echo "deploying World"'
            }  
        } 
    }
}
